const config = {
    defaults: {
        namespace: 'Application',
    },
    baseUrl: {
        dev: 'https://localhost:5001/',
        pro: 'https://vc.keenneed.com/',
    },
};

export default config;
