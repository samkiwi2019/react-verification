import React from 'react';
import { withRouter } from 'react-router-dom';

const RegisterPage = props => {
    return (
        <div>
            <h4> RegisterPage 1234</h4>
        </div>
    );
};

export default withRouter(RegisterPage);
